# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. Количество элементов массива значение которых меньше среднего арифметического всех элементов массива.

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2.  Печать массива
3.  Вычисление суммы элементов массива
4.  Поиск в массиве значения равного a
5.  Определение минимального значения среди элементов, находящихся после заданной позиции K.


## 📋 Задания к лаб. 16
Массив d0, d1, d2, ..., dh содержащий все элементы массива а0, а1, а2, ..., an,
расположенные до последнего отрицательного элемента в обратном порядке,
потом четные элементы b0, b1, b2 ..., bm и все элементы массива с

## 📁 Структура проекта

(например, )

lab-arrays/

├── src/

│   ├── arrays.h

│   ├── arrays.c

│   └── main.c

├── tests/

│   ├── test_arrays.c

│   └── run_tests.sh

├── Makefile

└── README.md


## 🔧 Реализованные функции

### 1. `double* full_elements(double *ptr_array, int n)`
- **Назначение**: Заполнение массива значениями
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `n` - размер массива
- **Возвращает**: указатель на заполненный массив

### 2. `int put_elements(double* ptr_array, int n)`
- **Назначение**: Печать элементов массива
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `n` - размер массива
- **Возвращает**: элементы массива с индексом

### 3. `double* calc_elements(double* ptr_array, int n)`
- **Назначение**: Обработка элементов массива
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `n` - размер массива
- **Возвращает**: новый массив, содержащий результат преобразования

### 3. `double sum_elements(double* ptr_array, int begin, int end)`
- **Назначение**: Вычисление суммы элементов массива  от индекса begin до индекса  end включительно
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `begin` - начальный индекс
  - `end` - конечеый индекс
- **Возвращает**: сумму элементов массива  от индекса begin до индекса  end включительно

### 4. `int find_element(double* ptr_array, int n, double element)`
- **Назначение**: Поиск элемента равного а
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `n` - размер массива
  - `element` - а
- **Возвращает**: индекс элемента равного а

### 5. `double find_min(double* ptr_array, int n, int k)`
- **Назначение**: Определение минимального значения среди элементов, находящихся после заданной
позиции K.
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `n` - размер массива
  - `element` - заданная позиция К
- **Возвращает**: минимальное значение после К
